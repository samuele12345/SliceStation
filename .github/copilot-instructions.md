# Copilot Instructions

## Linee guida del progetto
- L'utente preferisce usare la forma classica di jQuery `$.ajax({... success: ..., error: ...})` rispetto a `.done()`/`.fail()` per le chiamate AJAX.